# Polymorphism, method overriding (replace vs. extend) — solid

Built `BankAccount` v5 correctly, including the harder-than-usual core requirement (this day's baseline absorbed what would've been a stretch goal, per [[0004-inheritance-super]]): `minimum_balance` as a polymorphic class attribute overridden per-instance on `CheckingAccount`, `CheckingAccount.withdraw` as a full-replace override (no `super()`, correctly, since the parent's "reject if amount > balance" rule doesn't apply under overdraft), `SavingsAccount.deposit` as an extend override (calls `super().deposit(amount)` then adds a milestone message). Proved polymorphic dispatch with one loop calling `.withdraw()`/`.deposit()` on three different account types with zero type-checking — three different, correct outcomes. Completed the stretch goal (proving the milestone fires only for `SavingsAccount`) for the fifth day running, incidentally, as a natural side effect of the loop rather than a separate exercise. Confirmed verbally, unprompted, *why* `withdraw` needed full-replace and `deposit` could use extend — not just that they're different, but the actual reasoning (no reusable parent behavior vs. reusable parent behavior).

## Implications
- Method overriding (both replace and extend styles) and polymorphic dispatch are a solid floor. No need to re-teach.
- Confirmed pattern from [[0004-inheritance-super]]: stretch goals are being absorbed effortlessly (5/5 days). This learner's actual pace is running ahead of the original roadmap's per-day granularity in [[NOTES]] — consider compressing remaining days or merging adjacent roadmap topics (e.g. dunder methods + operator overloading, or SOLID SRP+OCP in one sitting) rather than continuing to escalate single-day scope indefinitely.
- Ready for Day 6: dunder/magic methods (`__str__`, `__repr__`, `__eq__`, `__lt__`) — a fresh, mostly-new-vocabulary topic rather than a further stretch on inheritance, which is a good place to reset pacing expectations before deciding whether to compress the rest of the path.
